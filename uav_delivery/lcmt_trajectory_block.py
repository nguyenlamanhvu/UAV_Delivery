"""LCM type definitions
This file automatically generated by lcm.
DO NOT MODIFY BY HAND!!!!
"""


from io import BytesIO
import struct

class lcmt_trajectory_block(object):

    __slots__ = ["trajectory_name", "num_points", "num_datatypes", "time_vec", "datapoints", "datatypes"]

    __typenames__ = ["string", "int32_t", "int32_t", "double", "double", "string"]

    __dimensions__ = [None, None, None, ["num_points"], ["num_datatypes", "num_points"], ["num_datatypes"]]

    def __init__(self):
        self.trajectory_name = ""
        """ LCM Type: string """
        self.num_points = 0
        """ LCM Type: int32_t """
        self.num_datatypes = 0
        """ LCM Type: int32_t """
        self.time_vec = []
        """ LCM Type: double[num_points] """
        self.datapoints = []
        """ LCM Type: double[num_datatypes][num_points] """
        self.datatypes = []
        """ LCM Type: string[num_datatypes] """

    def encode(self):
        buf = BytesIO()
        buf.write(lcmt_trajectory_block._get_packed_fingerprint())
        self._encode_one(buf)
        return buf.getvalue()

    def _encode_one(self, buf):
        __trajectory_name_encoded = self.trajectory_name.encode('utf-8')
        buf.write(struct.pack('>I', len(__trajectory_name_encoded)+1))
        buf.write(__trajectory_name_encoded)
        buf.write(b"\0")
        buf.write(struct.pack(">ii", self.num_points, self.num_datatypes))
        buf.write(struct.pack('>%dd' % self.num_points, *self.time_vec[:self.num_points]))
        for i0 in range(self.num_datatypes):
            buf.write(struct.pack('>%dd' % self.num_points, *self.datapoints[i0][:self.num_points]))
        for i0 in range(self.num_datatypes):
            __datatypes_encoded = self.datatypes[i0].encode('utf-8')
            buf.write(struct.pack('>I', len(__datatypes_encoded)+1))
            buf.write(__datatypes_encoded)
            buf.write(b"\0")

    @staticmethod
    def decode(data: bytes):
        if hasattr(data, 'read'):
            buf = data
        else:
            buf = BytesIO(data)
        if buf.read(8) != lcmt_trajectory_block._get_packed_fingerprint():
            raise ValueError("Decode error")
        return lcmt_trajectory_block._decode_one(buf)

    @staticmethod
    def _decode_one(buf):
        self = lcmt_trajectory_block()
        __trajectory_name_len = struct.unpack('>I', buf.read(4))[0]
        self.trajectory_name = buf.read(__trajectory_name_len)[:-1].decode('utf-8', 'replace')
        self.num_points, self.num_datatypes = struct.unpack(">ii", buf.read(8))
        self.time_vec = struct.unpack('>%dd' % self.num_points, buf.read(self.num_points * 8))
        self.datapoints = []
        for i0 in range(self.num_datatypes):
            self.datapoints.append(struct.unpack('>%dd' % self.num_points, buf.read(self.num_points * 8)))
        self.datatypes = []
        for i0 in range(self.num_datatypes):
            __datatypes_len = struct.unpack('>I', buf.read(4))[0]
            self.datatypes.append(buf.read(__datatypes_len)[:-1].decode('utf-8', 'replace'))
        return self

    @staticmethod
    def _get_hash_recursive(parents):
        if lcmt_trajectory_block in parents: return 0
        tmphash = (0x3071d11e402bfb0a) & 0xffffffffffffffff
        tmphash  = (((tmphash<<1)&0xffffffffffffffff) + (tmphash>>63)) & 0xffffffffffffffff
        return tmphash
    _packed_fingerprint = None

    @staticmethod
    def _get_packed_fingerprint():
        if lcmt_trajectory_block._packed_fingerprint is None:
            lcmt_trajectory_block._packed_fingerprint = struct.pack(">Q", lcmt_trajectory_block._get_hash_recursive([]))
        return lcmt_trajectory_block._packed_fingerprint

    def get_hash(self):
        """Get the LCM hash of the struct"""
        return struct.unpack(">Q", lcmt_trajectory_block._get_packed_fingerprint())[0]

